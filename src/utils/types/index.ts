export type navpropsData = {
  icon: React.ReactNode;
  text?: string;
  titleText?: string;
  route: string;
  color?: string;
  size?: string;
};

export type userData = {
  name: string;
  email: string;
  password: string;
  phoneNumber: number;
  address: string;
};
