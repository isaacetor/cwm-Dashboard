export type navpropsData = {
  icon: React.ReactNode;
  text?: string;
  titleText?: string;
  route: string;
  color?: string;
  size?: string;
};

export type userData = {
  email: string;
  address: string;
  ward: string;
  password: string;
};
